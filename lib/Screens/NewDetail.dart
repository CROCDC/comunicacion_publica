import 'package:comunicacion_publica/Model/Noticia.dart';import 'package:flutter/material.dart';import 'package:comunicacion_publica/Strings.dart';class NewDetail extends StatelessWidget {  final Noticia noticia;  const NewDetail({Key key, this.noticia}) : super(key: key);  @override  Widget build(BuildContext context) {    return Scaffold(        appBar: AppBar(            title: Text(appName),          ),        body: Container(          child: Column(            children: <Widget>[              Image.network(noticia.img),              Text(noticia.title),            ],          ),        ));  }}